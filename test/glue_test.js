const glue = require('../src/glue');
const should = require('should');

describe('../main.js', function () {

    it('test', function () {
        console.log(glue)
        //glue.should.equal(22)
    })


})
